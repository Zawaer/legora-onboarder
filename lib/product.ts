/**
 * The product's name and one-line pitch, in one place.
 *
 * These appear on /pay, /loi and in page metadata. When the positioning shifts
 * on Saturday night — and it always does — renaming should be one edit here
 * rather than a hunt through five files for the one that got missed, which is
 * always the one a customer sees.
 */

/** What it does, said plainly. The buyer is a Head of People, not a builder. */
export const PRODUCT = "Vanav";

/** One line. What they get for the money. Under about ten words. */
export const PITCH = "Onboards new hires into roles nobody has held before.";
